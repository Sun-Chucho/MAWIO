{
  "rules": {
    ".read": false,
    ".write": false,
    "mawio": {
      "$tier": {
        ".read": "$tier === 'standard' || $tier === 'platinum'",
        ".write": "$tier === 'standard' || $tier === 'platinum'"
      }
    }
  }
}
